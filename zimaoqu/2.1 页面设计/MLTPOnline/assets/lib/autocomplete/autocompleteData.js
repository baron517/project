/*  联想下拉数据源  */
define(['common', 'autocomplete'], function (common) {
    var autocompleteData = {
        //GetAreaPlaceByLevelWith123: function () {
        //    var resultData;
        //    common.Ajax.read(null,  "Canton", "GetCantonByLevelWith123", { "async": false }, function (result) {
        //        resultData = result;
        //        for (var i = 0; i < resultData.length; i++) {
        //            resultData[i]["FormatCantonFromLevelOne"] = decodeURIComponent(resultData[i]["FormatCantonFromLevelOne"]);
        //            resultData[i]["LevelOneID"] = decodeURIComponent(resultData[i]["LevelOneID"]);
        //            resultData[i]["LevelTwoID"] = decodeURIComponent(resultData[i]["LevelTwoID"]);
        //            resultData[i]["LevelThreeID"] = decodeURIComponent(resultData[i]["LevelThreeID"]);
        //        }
        //    });
        //    return resultData;
        //},
        //GetBank: function () {
        //    var resultData;
        //    common.Ajax.read(null,  "Bank", "GetList", { "async": false }, function (result) {
        //        resultData = result;
        //        for (var i = 0; i < resultData.length; i++) {
        //            resultData[i]["text"] = decodeURIComponent(resultData[i]["BankName"]);
        //            resultData[i]["id"] = decodeURIComponent(resultData[i]["BankID"]);
        //        }
        //    });
        //    return resultData;
        //}
    }
    return autocompleteData;
});